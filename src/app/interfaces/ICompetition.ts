/**
 * Interface representing a competition.
 * 
 * @interface ICompetition
 */
export interface ICompetition {
    // The name of the competition.
    name: string;
    // The unique code of the competition.
    code: string;
}